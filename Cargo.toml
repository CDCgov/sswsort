[workspace]
members = [
  "crates/sswsort",
  "crates/sswsort-cli",
]
resolver = "2"

[workspace.dependencies]
zoe = { version = "0.0.26", default-features = false, features = [
    "multiversion",
] }

[profile.prod]
inherits = "release"
codegen-units = 1
lto = "fat"
